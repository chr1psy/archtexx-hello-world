{
  "name": "vmodel-workspace",
  "version": "0.0.1",
  "private": true,
  "description": "V-Model pipeline workspace. Code is generated by Pipeline 2 agents.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}
